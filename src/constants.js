// Not a secret — just the Mapbox style (theme) to render. Change freely.
export const MAPBOX_STYLE = "mapbox://styles/robertegelund/ck66n56dx00r31in057j6r7qt";

// Denominator for the per-country percentage shown when a country is
// clicked. Paired with the still-static, MNOK-denominated per-country
// figures in data/africa.geojson (see aid-totals.js for the live,
// million-USD totals used everywhere else) - do not mix the two.
export const STATIC_TOTAL_AID_AFRICA_MNOK = 142624;
